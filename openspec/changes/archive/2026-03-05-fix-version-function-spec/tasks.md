## 1. Fix test function name

- [x] 1.1 Update `tests/linny_spec.lua:13` to call `linny_version#PluginVersion` instead of `linny_version#version`

## 2. Verify

- [x] 2.1 Run the test and confirm it passes
